"use client";
import { useState } from "react";
import { Heart, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
const amounts=[500,1000,2500,5000];
export default function DonatePage(){const [amount,setAmount]=useState(1000);return <section className="container grid gap-12 pb-24 pt-40 lg:grid-cols-2"><div><p className="eyebrow">Give with confidence</p><h1 className="mt-4 font-display text-5xl font-semibold text-primary">Your generosity reaches farther.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-ink-soft">Choose a programme, make a secure contribution, and receive a receipt for your records.</p><div className="mt-10 rounded-3xl bg-primary p-7 text-canvas"><Heart className="h-7 w-7 text-secondary"/><p className="mt-4 font-display text-2xl">Every contribution supports a community-led solution.</p></div></div><div className="rounded-3xl border border-line bg-canvas p-7 shadow-soft sm:p-10"><h2 className="font-display text-3xl font-semibold text-primary">Make a donation</h2><p className="mt-2 text-sm text-ink-soft">Secure payments via Razorpay, Stripe or UPI.</p><div className="mt-7 grid grid-cols-2 gap-3">{amounts.map(x=><button key={x} onClick={()=>setAmount(x)} className={`rounded-xl border p-4 text-left font-semibold ${amount===x?"border-primary bg-primary text-canvas":"border-line"}`}>₹{x.toLocaleString("en-IN")}</button>)}</div><label className="mt-5 block text-sm font-medium">Other amount<input type="number" min="1" value={amount} onChange={e=>setAmount(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-line px-4 py-3"/></label><Button className="mt-6 w-full" size="lg">Continue securely</Button><p className="mt-5 flex gap-2 text-xs leading-5 text-ink-soft"><ShieldCheck className="h-4 w-4 shrink-0 text-secondary-600"/>Payment details are encrypted. A donation receipt is issued after successful payment.</p></div></section>}
